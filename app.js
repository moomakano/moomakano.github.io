const DB=JSON.parse(localStorage.bp6||'{"budget":3000000,"cats":["ที่ดิน","ถมที่","เคลียริ่ง","โครงสร้าง","เขื่อน","ถนน","ไฟฟ้า","เครื่องใช้ไฟฟ้า","อุปกรณ์ตกแต่ง","รั้วบ้าน","จัดสวน"],"items":[]}');const $=id=>document.getElementById(id);function save(){localStorage.bp6=JSON.stringify(DB)}function render(){$("cats").innerHTML=DB.cats.map(c=>`<div>${c}</div>`).join("");$("list").innerHTML=DB.items.map(i=>`<div>${i.name} ${i.amount}</div>`).join("")||"ยังไม่มีข้อมูล";save()}document.querySelectorAll("nav button").forEach(b=>b.addEventListener("click",()=>{document.querySelectorAll(".page").forEach(p=>p.classList.remove("active"));$(b.dataset).classList.add("active")}));$("addBtn").addEventListener("click",()=>{const n=prompt("ชื่อรายการ");const a=+prompt("จำนวนเงิน");if(n&&a){DB.items.push({name:n,amount:a});render();}});render();