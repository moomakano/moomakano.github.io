let total=5000000,spent=0;const list=document.getElementById("list");function fmt(n){return new Intl.NumberFormat("th-TH").format(n)}function refresh(){t.textContent=fmt(total);s.textContent=fmt(spent);r.textContent=fmt(total-spent);p.textContent=Math.round(spent/total*100)+"%"}function add(){const n=document.getElementById("n").value;const v=+document.getElementById("a").value;if(!n||!v)return;spent+=v;list.innerHTML+=`<tr><td>${n}</td><td>${fmt(v)}</td></tr>`;refresh();n.value="";a.value=""}refresh();